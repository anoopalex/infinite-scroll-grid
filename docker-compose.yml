services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      SEED_COUNT: 2000
    volumes:
      - server-data:/app/data

  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server

volumes:
  server-data:
